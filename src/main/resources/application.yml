server:
  port: 80

spring:
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/mybatisdb
        username: root
        password: root

mybatis:
  typeAliasesPackage: com.example.testfour.entity
  mapperLocations: classpath:mappers/*.xml
